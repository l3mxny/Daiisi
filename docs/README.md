# Daiisi docs

Developer-facing notes on how the two main screens work and where their data comes from.

- [`field-input.md`](./field-input.md) — the map/draw screen where a farmer creates and saves fields
- [`results.md`](./results.md) — the dashboard that ranks fields, explains why, and generates AI recommendations

These are implementation notes, not user docs — they assume you're reading the code alongside them.
